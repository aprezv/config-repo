spring:
    application:
        name: oauth-server
security:
    user:
       password: password
server:
    port: 9999

eureka:
  client:
    serviceUrl:
      defaultZone: ${vcap.services.eureka-service.credentials.uri:http://localhost:8761}/eureka/